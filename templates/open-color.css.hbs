/*
 *
 *  𝗖 𝗢 𝗟 𝗢 𝗥
 *  v {{version}}
 *
 *  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

:root {

/*  General
 *  ─────────────────────────────────── */

{{#each general as |color|}}
  --oc-{{color.name}}: {{color.hex}};
{{/each}}
{{#each colors as |color|}}


/*  {{capitalize color.name}}
 *  ─────────────────────────────────── */

{{#each color.hex as |hex index|}}
  --oc-{{color.name}}-{{index}}: {{hex}};
{{/each}}
{{/each}}

}
